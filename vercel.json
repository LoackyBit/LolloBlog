{
  "build": {
    "env": {
      "HUGO_VERSION": "0.147.8"
    }
  },
  "buildCommand": "hugo --buildDrafts --buildFuture",
  "outputDirectory": "public",
  "installCommand": "echo 'Hugo will be installed automatically'",
  "devCommand": "hugo server --buildDrafts --buildFuture --port $PORT"
}
